# 前置安装
```bash
sudo apt install bochs
sudo apt-get install libc6-dev-i386
sudo apt-get install bochs-x
sudo apt install nasm
sudo mkdir /mnt/floppy
#怎么运行不起，不会是因为7代cpu太老hyper-v不支持的原因吧(还有可能是win11的问题)
```